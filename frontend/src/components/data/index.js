/**
 * Dữ liệu mẫu / catalog tĩnh.
 * - `cars` — danh sách xe demo (trang Home khi API lỗi).
 * - `mockDashboard` — biểu đồ & bảng mẫu cho dashboard (import có chọn lọc khi cần).
 */

export { cars } from './cars';
export * from './mockDashboard';
